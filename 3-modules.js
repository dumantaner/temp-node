// Modules

// Common JS , every file is modeul ( by default)
// Modules - Encapsulated Code (only share minimum)

const names = require('./4-name');
const sayHi=require('./5-utils');
const data = require('./6-alternative');
require('./7-mindgrenade');

//  sayHi('susan');
//  sayHi(names.taner);
//  sayHi(names.eda);
